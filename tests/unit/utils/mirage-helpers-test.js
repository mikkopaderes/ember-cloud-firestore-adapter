import { module, test } from 'qunit';

// import { handleRouteResource } from 'dummy/utils/mirage-helpers';

module('Unit | Utility | mirage helpers', function() {
  module('handleRouteResource', function() {
    test('nothing to test', function(assert) {
      assert.expect(1);

      assert.ok(true);
    });
  });
});
