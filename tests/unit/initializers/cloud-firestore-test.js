import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Initializer | cloud firestore', function(hooks) {
  setupTest(hooks);

  test('nothing to test', function(assert) {
    assert.expect(1);

    // Arrange
    assert.ok(true);
  });
});
